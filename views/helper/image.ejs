<!-- 
<% imageHelper = (folder,imageName) =>{ %>
    let xhtml = ""; 
    if(imageName !== null || imageName !== ""){
        <img src="<%=folderAvatar+item.avatar%>" class="img-thumbnail" alt="<%= item.name%>"> 
    } else {
        <img src="<%=folderAvatar+item.avatar%>" class="img-thumbnail" alt="<%= item.name%>"> 
    }
     
  <% } %> -->